import { Vector2d } from './geometry';
import { Node, Size } from './types';
export interface NodeState {
    pos: Vector2d;
    size: Vector2d;
    offset?: Vector2d;
    isCollapsed: boolean;
}
export declare const adjust: (prev: Map<string, NodeState>, size: Size, nodes: Node[]) => Map<string, NodeState>;
